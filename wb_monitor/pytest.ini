; Свой конфиг, чтобы pytest не подхватывал pyproject.toml корневого
; репозитория (там --doctest-modules и прочее, нам не нужное).
; Запуск: pytest из папки wb_monitor/
[pytest]
asyncio_mode = auto
testpaths = tests
