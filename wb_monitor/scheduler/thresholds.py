"""Все пороги в одном месте — не хардкодим в jobs.py.

Доли — в виде дробей (0.10 = 10%), сравнение строгое: «упала БОЛЬШЕ чем
на 10%», ровно 10% алерт не даёт.
"""

# --- алерты клиентам (alert_check_job) ---

# обвал цены конкурента: цена упала более чем на 10% к прошлому снепшоту
PRICE_DROP_THRESHOLD = 0.10
# рост цены: симметрично, более чем на 10%
PRICE_UP_THRESHOLD = 0.10

# --- признаки сломанного парсера (health_check_job), алерт админу ---

# доля SKU, по которым запрос упал/не распарсился за один проход
SCRAPER_FAILURE_RATIO = 0.30
# доля успешных SKU с нулевым остатком за один проход: массовый «обнул» —
# это скорее изменившаяся разметка WB, чем реальное рыночное событие
SCRAPER_ZERO_STOCK_RATIO = 0.50
# сколько часов без единого прохода parse_job считается аварией
PARSE_STALE_HOURS = 26

# --- отчёт (excel_report) ---

# порог «Мало остатка» в колонке «Статус»
LOW_STOCK_QTY = 5
# порог подсветки изменения цены в отчёте (мягче алертового:
# в еженедельной сводке интересны и небольшие движения)
REPORT_PRICE_CHANGE_THRESHOLD = 0.05
