# [asyncio] обязателен: иначе на Apple Silicon (arm64) не ставится
# greenlet, без которого async-движок SQLAlchemy падает при первом же запросе
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
aiogram>=3.7
httpx>=0.27
openpyxl>=3.1
apscheduler>=3.10,<4
click>=8.1
python-dotenv>=1.0

# dev
pytest>=8
pytest-asyncio>=0.23
